site_name: tmplt
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: tmplt
      handlers:
        python:
          options:
            docstring_style: numpy